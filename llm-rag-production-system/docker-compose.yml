version: '3.9'
services:
  rag-api:
    image: python:3.11-slim
    working_dir: /workspace
    volumes:
      - ./:/workspace
    command: >
      bash -lc "pip install --no-cache-dir -r requirements.txt && python app/main.py"
    ports:
      - "8000:8000"
